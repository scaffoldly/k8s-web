{
  "name": "k8s-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "util",
    "angular",
    "react",
    "angular-tests",
    "react-tests"
  ],
  "scripts": {
    "fetch-specs": "tsx scripts/fetch-specs.ts",
    "generate": "yarn fetch-specs && yarn --cwd angular run generate && yarn --cwd react run generate",
    "build": "yarn --cwd react run build",
    "lint": "yarn --cwd angular run lint && yarn --cwd react run lint",
    "format": "yarn --cwd angular run format && yarn --cwd react run format",
    "test": "yarn --cwd angular-tests run test && yarn --cwd react-tests run test"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "glob": "^10.3.10",
    "orval": "^6.31.0",
    "prettier": "^3.2.4",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
